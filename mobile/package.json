{
  "name": "tellmoreai-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android/iOS shell (Capacitor) that loads the live TellMore AI web app in its own WebView, so it does not depend on the phone's default browser the way the TWA did. Isolated from the web app on purpose — nothing here is part of the Next.js/Vercel build.",
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.2",
    "@capacitor/camera": "^6.1.0",
    "@capacitor/core": "^6.1.2",
    "@capacitor/dialog": "^6.0.2",
    "@capacitor/geolocation": "^6.0.1",
    "@capacitor/push-notifications": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2",
    "sharp": "^0.33.5"
  }
}
