{
  "name": "explainer-video",
  "version": "1.0.0",
  "description": "Makes the two narrated explainer videos (English + Bangla, 1080p, ~5 min) from a script, without any paid tool: Microsoft Edge neural voices for the narration, Chrome (via Playwright) to record the animated scenes, ffmpeg to mux and join. Nothing here touches the web app; it is a standalone folder.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "gsap": "^3.15.0",
    "msedge-tts": "^2.0.7",
    "playwright-core": "^1.63.0",
    "three": "^0.186.0"
  }
}
